const mongoose = require('mongoose');

const EmprestimoSchema = mongoose.model('Emprestimo', {
    //idUsuario credor
    //nomeUsuario credor
    //idUsuario tomador
    //nomeUsuario tomador
    //status do emprestimo {}
    //valor
});

module.exports = EmprestimoSchema;